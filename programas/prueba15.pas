program prueba15;

const

   literal='Esto es un literal';
   N=4;
   PI=3.141592;

var
   primera:integer;
   segunda: real;
   tercera:boolean;

begin

   read(primera);
   read(segunda);
   write(primera);
   write(segunda);
   write(literal);
   write(N);
   write(PI)

end.